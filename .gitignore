# Cached GBIF API responses. These are regenerated by running the pipeline,
# and they can grow to hundreds of megabytes, so they never go into git.
data/raw/

# Python noise
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
